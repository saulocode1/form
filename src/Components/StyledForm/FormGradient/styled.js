import styled from 'styled-components';

const FormGradient = styled.div`
   background: linear-gradient(var(--gradient-1), var(--gradient-2)) ;
   position: absolute;
   width: 100%;
   height: 100%;
   opacity: 0.4;
`;

export default FormGradient;